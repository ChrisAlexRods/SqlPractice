 ALTER TABLE celebs 
 ADD COLUMN 
 twitter_handle TEXT; 

 SELECT * FROM
 celebs;

--  The ALTER COMMAND is used to add columns into a table in
-- conjunction with the table name. 

-- To add the specific colummn use ADD COLUMN then the column name 
-- name with the type. twitter_handle TEXT; 