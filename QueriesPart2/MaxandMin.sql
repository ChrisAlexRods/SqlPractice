SELECT MIN(downloads)
FROM fake_apps;

SELECT MAX(downloads)
FROM fake_apps;

-- Will show the highest and lowest downloaded apps. 

SELECT MAX(price)
FROM fake_apps;

-- This one shows me the highest price app in the table. 