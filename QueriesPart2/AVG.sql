SELECT AVG(downloads)
FROM fake_apps;

-- You wrap the column name you want the avg from AVG(). Similar to count. 

SELECT AVG(price)
FROM fake_apps;

-- For this query we are finding the average of the price of all apps in the database. 