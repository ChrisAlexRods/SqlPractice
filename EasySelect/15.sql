Write a query that prints a list of employee names (i.e.: the name attribute) from the Employee table in alphabetical order.

SELECT name
FROM Employee 
ORDER BY name

If you want to order something do it by 
the column name rather than ascending. 